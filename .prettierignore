# Build output and local tooling caches
dist/
.netlify/
.vite/

# Generated by scripts/image_metadata.py, which writes indent=4. Formatting
# these would only fail CI again the next time the galleries are regenerated.
public/gallery-img-metadata.json
public/gallery-shala-img-metadata.json

# Vendored verbatim from daisyui.com's llms.txt — keep it identical to upstream
# so re-downloading a newer version never shows up as a formatting diff.
.github/instructions/daisyui.instructions.md

# Generated lockfile (Netlify CLI)
deno.lock
